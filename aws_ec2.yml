plugin: amazon.aws.aws_ec2

regions:
  - ap-south-1

filters:
  tag:Role: ansible-target
  instance-state-name: running

hostnames:
  - public-ip-address

compose:
  ansible_host: public_ip_address
  ansible_user: "'ubuntu'"
  ansible_ssh_private_key_file: "'/home/meer/key/mongodb-key.pem'"
